# Docker

```bash
docker compose up --build
```

Open `http://127.0.0.1:8787`. The compose file binds only to host loopback and persists personal data in a named volume. Ollama is expected on the host at port 11434.
